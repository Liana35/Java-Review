package Review8;

/*
public class InsuranceTest {
    public static void main(String[] args) {
        //wwe cant create an object of an abstract class
        // we can create it indirectly by using a child class


       Insurance carPolicy= new CarPolicy("I94758","John Smith", 200, 22);
       //!!!! left side class is the methods that ar going to be available - Insurance , not carPolicy

        carPolicy.getInsurance();
        carPolicy.calculateCoverage();
        // calculateCoverage() has a return so we need to print it out
        System.out.println(carPolicy.calculateCoverage());



        //upcasting - creating object from Child class
       Insurance petPolicy= new PetPolicy("P44658","Olena L", 20, 4);
       petPolicy.getInsurance();
       petPolicy.getInsurance();
        System.out.println(petPolicy.calculateCoverage());


        Insurance[] insurances={
                new CarPolicy("I94758","John Smith", 200, 22),
                new CarPolicy("I94758","John Smith", 200, 22),
                new PetPolicy("P44658","Olena L", 20, 4),
                new CarPolicy("I94758","John Smith", 200, 22),
        };
        insurances[0].calculateCoverage();
    }
}
*/
